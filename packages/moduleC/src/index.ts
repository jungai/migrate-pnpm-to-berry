import { getTheBestSong } from "module-a";

const numberOne: string = "iu";

const everything = (secret: string) => `module C -> everything is ${secret}`;

console.log(everything("iu ❤️"));
console.log(getTheBestSong());
